def calculate_discount(item, price):
        # Simple example: 10% off if quantity >= 3
            if item.quantity >= 3:
                    return price * Decimal("0.90")
                        return price
                        